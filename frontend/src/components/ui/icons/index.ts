// Curated re-export of the Phosphor icons actually used across the app.
// Don't barrel-export the whole library here — add a name only when a
// screen genuinely needs it, and prefer duotone weight at the call site.
export {
  Farm,
  Plant,
  Leaf,
  Basket,
  Truck,
  Bell,
  CloudSun,
  CheckCircle,
  WarningCircle,
  UserCircle,
  Gear,
  Translate,
} from '@phosphor-icons/react'
